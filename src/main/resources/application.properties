spring.application.name=cse360project

# NOTE: use `create-drop` to always reset data, use `update` to persist data
# if you're going to persist you'll want to comment ou the bean in LoadDatabase.java
spring.jpa.hibernate.ddl-auto=create-drop
# spring.jpa.hibernate.ddl-auto=update

spring.h2.console.enabled=true
spring.h2.console.path=/console/